<%= form_for(@location_history) do |f| %>
  <% if @location_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@location_history.errors.count, "error") %> prohibited this location_history from being saved:</h2>

      <ul>
      <% @location_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pet_id %><br>
    <%= f.text_field :pet_id %>
  </div>
  <div class="field">
    <%= f.label :record_date %><br>
    <%= f.datetime_select :record_date %>
  </div>
  <div class="field">
    <%= f.label :latitude %><br>
    <%= f.text_field :latitude %>
  </div>
  <div class="field">
    <%= f.label :longitude %><br>
    <%= f.text_field :longitude %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
